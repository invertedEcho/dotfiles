- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/kitty: kitty
    ~/.config/git: git

# - create:
#     - ~/downloads
#     - ~/.vim/undo-history

- shell:
  - [git submodule update --init --recursive, Installing submodules]
